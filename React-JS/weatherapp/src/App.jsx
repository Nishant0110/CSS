import { useState } from 'react'
import reactLogo from './assets/react.svg'
import viteLogo from '/vite.svg'
import './App.css'

/*function App() {
  let [dataset, setdata] = useState([])
  let clickfunction = () => {
    fetch("https://jsonplaceholder.typicode.com/posts")
      .then(resdata => resdata.json())
      .then(jsonconverteddata => { console.log(jsonconverteddata); setdata(jsonconverteddata) })
  }

  return (
    <>
      <button onClick={clickfunction}>Click here</button>
      {dataset.length > 0 && (
        <>
          {
            dataset.map(mappeddata => (
              <>
                <p>id={mappeddata.id}</p>
                <p>title={mappeddata.title}</p>
              </>
            ))
          }
        </>
      )}
    </>
  );
}*/








export default App

